import { createStore } from "vuex";

import PageModules from "./modules/PageModules.js";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    PageModules,
  },
});
